<%= form_tag create_member_login_attemps_system_variables_path, method: :post do %>
  <table class='table table-hover table-striped' >
    <tr>
      <th style="width: 300px;"> <%= t("system_var.max_failed") %> </th>
      <td> <%= text_field :max_failed, '', value: @max_failed_login_attemps.try(:value), class: 'form-control', style: 'display: inline; width: 100%;', placeholder: t("public.example")+"：5" %></td>
    </tr>
    <tr>
      <th style="width: 300px;"> 资金密码允许操作失败最大次数 </th>
      <td> <%= text_field :assets_password_max_failed, '', value: @assets_password_max_failed.try(:value), class: 'form-control', style: 'display: inline; width: 100%;', placeholder: t("public.example")+"：5" %></td>
    </tr>
    <tr>
      <td colspan=10>
        <%= submit_tag t("public.confirm"), class: 'btn btn-primary', style: 'line-height: normal;' %>
      </td>
    </tr>
  </table>
<% end %>
<script>
  $("form").validate({
    rules: {
      "max_failed[]": {
        required: false,
        number: true,
        // member_true: true
      }
    }
  })
</script>
